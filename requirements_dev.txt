# Development dependencies for ha-radarcat.
#
# Set up the dev environment with:
#   uv venv --python 3.13 .venv
#   uv pip install --python .venv/bin/python -r requirements_dev.txt
#
# The integration's own runtime requirement is Pillow (manifest.json:
# requirements) — homeassistant already pulls it transitively, this pin
# just makes the version explicit for dev/CI too.

pytest==9.0.0
pytest-cov==7.0.0
pytest-homeassistant-custom-component==0.13.316
homeassistant==2026.2.3
ruff==0.16.1
aioresponses==0.7.9
Pillow==12.0.0
